// Enum represents fixed categories for bills
// Using enum prevents invalid category values like "TV" or "Food123"

public enum Category {
    RENT,
    ELECTRICITY,
    INTERNET,
    PHONE,
    CREDIT_CARD,
    SUBSCRIPTION,
    OTHER
}